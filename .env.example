# Optional: used only when running locally
# Copy to .env and adjust values.

# Total cluster capacity per GPU type.
SCHEDULER_CAPACITY_JSON='{"A100":4,"H100":2}'

# Per-tenant quota caps per GPU type.
# GPU types must exist in SCHEDULER_CAPACITY_JSON.
SCHEDULER_TENANT_QUOTAS_JSON='{"t1":{"A100":2,"H100":1},"t2":{"A100":2,"H100":1}}'